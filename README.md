# Islam Web Studio

Bilingual Laravel + Filament + Livewire studio CMS.

See `docs/PROJECT-HANDBOOK.ar.md` for the full Arabic operating guide, module fields, demo data and launch checklist. See `PROJECT-SPEC.md` for the original brief.

Local startup: MySQL in Laragon, `start-server.ps1` and `start-worker.ps1`. Tests: `run-tests.ps1`. The existing `.env` is configured for this machine; do not publish it. `.env.example` contains no credentials.

The final requested folder is `C:\laragon\www\islam web`. See `docs/PROJECT-LOCATION.md` for the verified installation status.
