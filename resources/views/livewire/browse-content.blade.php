<div><div class="archive-filters"><div class="filter-chips"><button type="button" wire:click="$set('category','')" @class(['active'=>$category===''])>@t('all')</button>@foreach($categories as $cat)<button type="button" wire:click="$set('category','{{ $cat->id }}')" @class(['active'=>$category===(string)$cat->id])>{{ $cat->titleText() }}</button>@endforeach</div><div class="search-field"><input type="search" wire:model.live.debounce.400ms="search" maxlength="100" placeholder="{{ \App\Support\Studio::text('search') }}…" aria-label="{{ \App\Support\Studio::text('search') }}"><span aria-hidden="true">⌕</span></div></div><div wire:loading.delay class="loading-line" role="status">@t('saving')</div><div class="{{ $module==='services'?'services-grid':($module==='projects'?'projects-grid':'posts-grid') }}" wire:loading.class="is-loading">@forelse($records as $item)@if($module==='services')<x-service-card :service="$item" :number="$loop->iteration" wire:key="service-card-{{ $item->id }}" />@elseif($module==='projects')<x-project-card :project="$item" wire:key="project-card-{{ $item->id }}" />@else<x-post-card :post="$item" wire:key="post-card-{{ $item->id }}" />@endif @empty<p class="empty-state">@t('no_records')</p>@endforelse</div><div class="pagination">{{ $records->links() }}</div></div>