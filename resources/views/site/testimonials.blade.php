@extends('layouts.site')
@section('content')<section class="page-intro shell"><x-page-eyebrow :label="$heading['label']" /><h1>{{ $heading['title'] }}</h1>@if(filled($heading['description']))<p>{{ $heading['description'] }}</p>@endif</section><section class="section shell"><div class="testimonials-grid">@foreach($testimonials as $review)<x-testimonial :review="$review" />@endforeach</div><div class="pagination">{{ $testimonials->links() }}</div></section>@endsection
