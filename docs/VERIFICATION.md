# Verification — Islam Web Studio

Validated on 2026-09-03 in the local Windows/Laragon environment.

- PHPUnit: **29 tests passed; 341 assertions**. Isolated MySQL test database; application/demo database untouched by tests.
- Vite production build: passed (native configuration loader on Windows).
- Pint: PHP source formatted successfully.
- `artisan optimize`: config, events, routes, views, Blade icons and Filament caches built successfully. Caches cleared before copying to avoid references to the staging path.
- Browser: Arabic home at 320px and 1280px; English home at 1440px; English service at 320px; portfolio at 375px/768px. No horizontal document overflow in those checks. Arabic services table/cards and date-filter modal tested at 320px.
- Home services: Arabic and English were visually checked at 1280×720. The compact grid is capped at 1040px, uses 22px gutters, and renders each service image in full with `object-fit: contain`. At 1280px the cards measured about 332×497px with no horizontal overflow. The section title is an H3 with its description directly below and the existing View all link opposite. Card-number labels and service icons remain removed; reduced-motion users receive the static layout.
- Home work showcase: the original light design and equal project-card structure were restored. Arabic was visually checked at 1280×720: the Home page rendered exactly 9 cards in a 3-column grid, each about 368px wide, with 24px column gutters and no horizontal overflow. The fixed demo badge was removed from project covers; browser inspection confirmed zero demo-badge elements and zero project-cover text matches for «نموذج تجريبي». The Home query is capped at 9; tablet and mobile collapse to 2 and 1 columns respectively. Automated coverage confirms the 9-card limit and guards against the removed asymmetric showcase variant.
- Browser interactions: admin login, bilingual navigation, live search, featured toggle and restoration, inclusive date range, filtered Excel generation through the queue, mobile dropdown services, gallery open/next/close, and menu order controls.
- Home Hero: Arabic and English were visually checked at 1280×720. The media height matched the viewport, Arabic content aligned right, English content aligned left, and video sources remained deferred. The title scale was reduced across desktop, tablet and mobile while remaining an H3; browser inspection measured the Arabic desktop title at 60.16px on a 1280px viewport. The Home navbar started transparent and became solid/fixed after scrolling 80px. The About page started with a solid/fixed navbar and had no Home Hero. Automated coverage also confirms the display-location field is absent and activating a slider group disables the previous group.
- Home introduction settings: the Home settings tab now controls section and media-panel visibility, visual media selection/upload, cover/contain fitting, media background, decorative frame, bilingual media caption and caption color, eyebrow, title, description, bilingual link text and URL, new-tab behavior, founder label and signature. The public view keeps the existing brand mark as a fallback until a media asset is selected. Filament form assertions confirmed the controls exist, and a public feature test changed every value, rendered the chosen asset and custom URL, verified that explicitly cleared nullable translations stay hidden instead of falling back to demo copy, then disabled the section successfully. Browser inspection of the current database confirmed both the image caption and founder label were absent.
- Dynamic pages, gallery and removed CTA: the About page and every standard page use the same compact managed layout: responsive title/excerpt, rich content beside the full uncropped hero image/video, then gallery images, videos and file downloads. The gallery is capped at 1080px; at a 1280px viewport each two-column card measured about 528px. Captions render centered at 18px with a small animated brand accent; cards use hover elevation plus alternating side/up/down reveal directions. Page videos use branded custom play/pause, progress, time, mute and fullscreen controls with native controls disabled. Browser inspection measured a 40.96px Arabic title, matching natural/rendered image ratios, no horizontal overflow, working video progress and no console errors. The founder card and lower-page CTAs remain removed. Automated coverage checks standard/About layouts, custom controls, reveal directions, all gallery media types and the removed settings.
- Localized admin recovery: `/ar/admin` redirects to `/admin`, `/en/admin/services` redirects to `/admin/services`, and guest access continues to Filament's `/admin/login`. Browser inspection confirmed the Arabic login form loads instead of the public 404 page.
- Visual media picker: image thumbnails appeared inside the service image dropdown; the in-picker upload modal exposed file preview, Arabic/English names and alternative text. Automated checks covered both `<img>` and `<video>` options and immediate creation of a selectable library asset.
- Admin typography and icons: the Arabic dashboard was visually checked after the redesign. Small text uses locally bundled 500/600/700 font files and exposes validated weight and size selectors under dashboard design settings. Sidebar modules render distinct semantic icons; language, website, notification and user controls use a consistent topbar button design. Invalid stored typography values fall back to safe defaults.
- Font selection and topbar spacing: the topbar was visually rechecked with a 12px separation between the locale/site group and account group, plus approximately 14px between the account and notification controls. Arabic settings list IBM Plex Sans Arabic, Cairo and Tajawal; English heading settings list Cormorant Garamond, DM Sans, Inter and Poppins. All font files are locally bundled, settings are allow-listed, and automated checks confirm the selected CSS tokens are emitted.
- Filament font application: the panel no longer hard-codes IBM Plex Sans Arabic. Its locale-aware font family resolves through the same allow-listed setting, and a settings save schedules one full reload so newly selected font assets and root variables are applied immediately. Browser inspection confirmed Cairo for the admin root, body and heading, with `document.fonts.check('16px Cairo')` returning true.
- Notifications: Filament database notification bell, unread counter, 10-second dynamic refresh, request action link, and public Livewire toast dispatch were verified. The notifications migration ran successfully on the application database.
- Immediate media availability: uploads from both the main media resource and the visual picker now ingest the original before the response completes. Responsive WebP/AVIF work runs after the response without a queue worker. Existing assets 11 and 12 were processed successfully and appeared as selectable thumbnails; pending and failed job counts were both zero afterward.
- Video: active desktop video observed playing muted, hidden mobile counterpart paused, no media error reported.
- Seeded/current demo data: the seeder defines 9 services and 9 demo projects; the current working database contains 10 projects including existing user content, while Home intentionally displays at most 9. It also currently contains 6 posts, 5 pages, 4 testimonials, 5 methodology steps, 26 leads, 25 assets, 1 active Home Hero group / 4 slides, 23 menu items, 3 locations and 2 completed sample exports. Build health snapshot in `build-health.json`.
- No pending or failed queue jobs in the checked demo state.
- Original existing `islam-web-studio` project and original `islam_web_studio` database were not modified.

See `PROJECT-LOCATION.md` for the final copy and startup result.

No public deployment, SMTP delivery, advertising account connection, full cross-browser/device matrix or Lighthouse/Core Web Vitals benchmark is claimed. Demo content, approximate SVG logo recreation, and other operational limits are documented in `PROJECT-HANDBOOK.ar.md`.

`islam-web-source.zip` is a portable source archive without credentials, vendor or node_modules. It is not a backup of the running MySQL database. The local installed project contains dependencies and its private configured `.env`.
